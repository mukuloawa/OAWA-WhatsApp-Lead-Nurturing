# Same as the real config/optout.yaml — duplicated here so scenario
# tests don't depend on settings.config_dir pointing at the repo root.
patterns:
  - "stop"
  - "unsubscribe"
  - "band karo"
  - "mat bhejo"
  - "don't message"
  - "do not message"
  - "remove me"
  - "opt out"
  - "optout"
